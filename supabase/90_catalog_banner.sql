-- 90: фон карточки в каталогах (v1.335.0).
--
-- Одной аватарки мало: в каталоге из двух десятков плиток они все выглядят
-- одинаково — квадратик, название, строчка. Фон делает плитку узнаваемой с
-- расстояния, как обложка у сервера. Необязателен: без него рисуется мягкая
-- заливка от акцентного цвета, и плитка не выглядит пустой.
alter table public.plugin_catalog add column if not exists banner_url text;
alter table public.bot_catalog    add column if not exists banner_url text;
