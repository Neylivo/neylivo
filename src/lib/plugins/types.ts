// v1.286.0: система плагинов. Плагин — это один .ponoi-файл (обычный JS с шапкой
// метаданных), который выполняется в Web Worker'е и общается с приложением только
// через явно выданный API (см. sandbox.ts / api.ts).
//
// Почему Worker, а не просто eval в окне: сессия Supabase лежит в localStorage, а
// Worker к localStorage/document.cookie/DOM доступа не имеет в принципе — это
// ограничение самого браузера, а не наша проверка, которую можно обойти хитрым
// кодом. Поэтому плагином, скачанным из чата, нельзя угнать аккаунт, даже если он
// написан специально для этого.

/** Что плагин просит разрешить. Показывается человеку при установке. */
export type Permission =
  | 'ui'              // свои кнопки и пункты меню
  | 'css'             // свои стили
  | 'commands'        // свои слэш-команды
  | 'messages.read'   // видеть сообщения в открытом канале
  | 'messages.write'  // отправлять сообщения от твоего имени
  | 'storage'         // своё хранилище ключ-значение
  | 'net'             // сеть, только на домены из @hosts
  | 'settings'        // своя страница настроек
  | 'notify'          // всплывающие уведомления
  | 'voice'           // переключать эффект своего голоса в звонке
  | 'context'         // знать твоё имя и какой канал открыт
  | 'panel'           // своя панель в плеере, Трекотеке, чате или колонке слева
  | 'music'           // видеть, что играет, и управлять плеером
  | 'navigate'        // переключать открытый канал и диалог
  | 'status'          // менять твою активность («Играет в…»)
  // v1.465.0: четыре новых.
  | 'ipc'             // говорить с другими плагинами
  | 'messages.intercept' // менять сообщения до отправки и до показа
  | 'background'      // работать по расписанию, когда его панель закрыта
  | 'ui.theme'        // менять цвета оформления
  | 'apps'            // своя область экрана: окно, вкладка, полный экран
  // v1.473.0
  | 'input'           // видеть геймпады, подключённые к устройству
  // v1.475.0
  | 'messages.upload' // видеть и МЕНЯТЬ файлы до отправки

export const ALL_PERMISSIONS: Permission[] = [
  'ui', 'css', 'commands', 'messages.read', 'messages.write', 'storage', 'net', 'settings', 'notify', 'voice', 'context',
  'panel', 'music', 'navigate', 'status',
  'ipc', 'messages.intercept', 'background', 'ui.theme', 'apps', 'input', 'messages.upload',
]

/** Человеческое описание разрешения — ровно то, что читает человек перед установкой. */
export const PERMISSION_LABEL: Record<Permission, string> = {
  'ui': 'Добавлять кнопки и пункты меню',
  'css': 'Менять оформление приложения',
  'commands': 'Добавлять команды в чат',
  'messages.read': 'Видеть сообщения в открытом канале',
  'messages.write': 'Отправлять сообщения от твоего имени',
  'storage': 'Хранить свои данные на этом устройстве',
  'net': 'Обращаться в интернет (только к указанным сайтам)',
  'settings': 'Своя страница настроек',
  'notify': 'Показывать уведомления',
  // v1.360.0: имя и открытый канал. Само по себе безобидно — всё это и так на
  // экране, — но вместе с «net» это уже то, что может уехать на чужой сайт,
  // поэтому разрешение отдельное и видно при установке.
  'context': 'Знать твоё имя и какой канал сейчас открыт',
  // v1.333.0: плагин выбирает эффект, но самого звука не получает — обработка
  // целиком в приложении (src/lib/voiceFx.ts). Формулировка ровно об этом,
  // чтобы никто не решил, будто плагин слышит разговор.
  'voice': 'Менять эффект твоего голоса в звонке (сам звук ему не доступен)',
  // v1.417.0: своя панель и музыка. Панель — это описание строк, которые рисует
  // само приложение: чужой разметки в окне не появляется. Музыка — то же, что
  // человек делает кнопками, плюс список Трекотеки, который и так на экране.
  'panel': 'Своя панель в плеере, Трекотеке, чате или колонке слева',
  'music': 'Видеть, что играет, и управлять плеером',
  // v1.419.0: переход по приложению и своя активность. Оба — то же, что человек
  // делает сам: щёлкнуть по каналу и написать о себе строку в настройках.
  // Отдельные разрешения потому, что оба ЗАМЕТНЫ: первое уводит с открытого
  // экрана, второе видно другим людям рядом с твоим ником.
  'navigate': 'Открывать каналы и диалоги вместо тебя',
  'status': 'Менять твою активность, которую видят другие',
  // v1.465.0: разговор между плагинами. Наружу это не выходит вообще: данные
  // ходят между двумя воркерами на этом же устройстве, и оба конца обязаны иметь
  // это разрешение — иначе плагин получал бы чужие данные, не спросив никого.
  'ipc': 'Обмениваться данными с другими твоими плагинами',
  // Самое сильное из всех. Плагин видит КАЖДОЕ сообщение до отправки и может его
  // переписать — то есть отправить не то, что человек набрал. Формулировка
  // нарочно прямая: это не «форматирование», это подмена.
  'messages.intercept': 'Читать и МЕНЯТЬ твои сообщения до отправки и до показа',
  'background': 'Работать по расписанию, даже когда его окно закрыто',
  'ui.theme': 'Менять цвета оформления приложения',
  // v1.471.0: своя область экрана. Формулировка честная про самое неприятное —
  // окно во весь экран. Подделать им приложение всё же нельзя: шапка с именем
  // плагина и кнопкой закрытия рисуется нами и убрать её плагин не может.
  'apps': 'Открывать своё окно, вкладку или полный экран поверх приложения',
  // v1.473.0: геймпад. Само по себе безобидно — это устройство, которое человек
  // подключил сам, и плагин видит только нажатия, — но название геймпада вместе
  // с «net» это ещё одна примета устройства, поэтому разрешение отдельное.
  'input': 'Видеть геймпады и джойстики, подключённые к устройству',
  // v1.475.0: перехват вложений. Формулировка нарочно прямая: это не
  // «обработка файлов», это возможность отправить не тот файл, который человек
  // выбрал. Ради этого же оно и нужно — снять с фотографии геометку.
  'messages.upload': 'Видеть и МЕНЯТЬ файлы, которые ты отправляешь',
}

/**
 * Разрешения, о которых стоит предупредить отдельно: остальные ничего о тебе не
 * узнают и ничего от твоего имени не сделают, а эти — могут. На экране установки
 * они подсвечиваются, чтобы «отправлять сообщения от твоего имени» не терялось в
 * одном списке с «добавлять кнопки».
 */
export const SENSITIVE_PERMISSIONS: Permission[] = [
  'messages.read', 'messages.write', 'net',
  // v1.465.0. Перехват — сильнее всего в этом списке: он и читает переписку, и
  // подменяет отправляемое. Фоновая работа — потому что плагин действует тогда,
  // когда человек на него не смотрит и не свяжет странность с ним.
  'messages.intercept', 'background',
  // v1.475.0: перехват вложений — плагин видит каждый отправляемый файл.
  'messages.upload',
]

/**
 * События, на которые плагин может подписаться (v1.397.0).
 *
 * Раньше событие было ровно одно — «пришло сообщение». Всё остальное, что
 * происходит в приложении, плагину было не видно: он не мог узнать даже о том,
 * что человек перешёл в другой канал. Отсюда и брались плагины, которые
 * опрашивают всё подряд по таймеру.
 *
 * Таблица — единственный источник правды: по ней и выдаётся разрешение при
 * подписке (api.ts), и пишется документация (spec.ts), и проверяется, что ни
 * одно событие не завелось мимо разрешения (__attack_test.ts). Ставить событие
 * без строки в этой таблице нельзя: подписка на неизвестное имя отвергается.
 */
export interface PluginEventSpec {
  /** Без какого разрешения подписка отвергается. null — событие безобидно. */
  permission: Permission | null
  /** Что приходит в обработчик — эта же строка идёт в документацию. */
  payload: string
  /** Когда случается — человеческим языком, для документации. */
  when: string
}

export const PLUGIN_EVENTS: Record<string, PluginEventSpec> = {
  'message': {
    permission: 'messages.read',
    payload: '{ id, author, authorName, content, mine, mentionsMe }',
    when: 'пришло новое сообщение в открытом канале',
  },
  'message.edit': {
    permission: 'messages.read',
    payload: '{ id, author, content }',
    when: 'сообщение в открытом канале поправили',
  },
  'message.delete': {
    // Уходит только id: содержимое удалённого сообщения плагину не нужно, а
    // отдавать его — значит отдавать ровно то, что человек только что убрал.
    permission: 'messages.read',
    payload: '{ id }',
    when: 'сообщение в открытом канале удалили',
  },
  'reaction': {
    permission: 'messages.read',
    payload: '{ messageId, emoji, userId, added }',
    when: 'на сообщение поставили или сняли реакцию',
  },
  'channel': {
    permission: 'context',
    payload: '{ id, name, serverId, kind }',
    when: 'человек перешёл в другой канал или личку',
  },
  'voice': {
    permission: 'context',
    payload: '{ room, joined }',
    when: 'ты зашёл в разговор или вышел из него — сам звук плагину недоступен',
  },
  'typing': {
    permission: 'messages.read',
    payload: '{ name }',
    when: 'кто-то печатает в открытом канале',
  },
  // v1.419.0: своя настройка. Событие существовало с самого начала — его шлёт
  // экран настроек плагина и его панель (emitToPlugin), — но в этой таблице его
  // не было, и ponoi.on('settings') ОТВЕРГАЛСЯ как неизвестное имя. Обработчик
  // при этом работал: подписка в песочнице ставится до обращения к приложению.
  // То есть плагин, написанный по нашей же документации и по нашей же
  // заготовке, получал красную строку «Неизвестное событие «settings»» на
  // карточке — при полностью рабочем плагине.
  //
  // Разрешения не требует намеренно: приходят только СВОИ настройки, которые
  // человек только что тронул на странице этого же плагина.
  'settings': {
    permission: null,
    payload: '{ key, value }',
    when: 'человек изменил строку на странице настроек или в панели этого плагина',
  },
  // v1.419.0: музыка. Раньше плагин мог только спросить music.now() — то есть
  // опрашивать плеер по таймеру, чего не должен делать никто.
  'music': {
    permission: 'music',
    payload: '{ id, title, author, playing }',
    when: 'сменился трек, нажали паузу или продолжили',
  },
  // v1.465.0: письмо от другого плагина. Разрешение нужно ОБОИМ концам: у
  // отправителя оно проверяется при вызове, у получателя — вот здесь, при
  // подписке. Иначе плагин, ни о чём не просивший, начал бы получать данные от
  // соседа, а человек об этом даже не узнал бы.
  //
  // from подставляет приложение, а не отправитель: подделать имя нельзя.
  'ipc': {
    permission: 'ipc',
    payload: '{ from, event, data }',
    when: 'другой твой плагин прислал данные через ponoi.plugins.send',
  },
  // Холст готов к рисованию. Событие обязательно: холст живёт в панели, а панель
  // человек открывает и закрывает — до открытия рисовать просто некуда, и плагин
  // должен узнать момент, а не опрашивать по таймеру.
  // Холст виден или спрятан. Событие не про «холст пропал» — он не пропадает,
  // рисовать в него можно всегда. Оно про то, ВИДИТ ли это человек: пока панель
  // закрыта, крутить анимацию бессмысленно, и плагин должен иметь возможность
  // её остановить, а не жечь батарею вхолостую.
  'canvas': {
    permission: 'panel',
    payload: '{ key, width, height, visible }',
    when: 'панель с холстом появилась на экране или ушла с него',
  },
  // v1.467.0: системные события. Все три — про состояние САМОГО приложения, а
  // не про человека и не про переписку: они ничего о нём не рассказывают, и
  // разрешения поэтому не требуют.
  //
  // Смысл у них один и очень практический: плагину нужно знать, когда можно
  // перестать работать. Крутить анимацию в свёрнутом окне или пересчитывать
  // цвета, не зная о смене темы, — это севшая батарея и вид «плагин сломался».
  'theme': {
    permission: null,
    payload: '{ theme, light, accent }',
    when: 'человек сменил тему оформления (тёмная, светлая, своя)',
  },
  'focus': {
    permission: null,
    payload: '{ focused }',
    when: 'окно приложения стало активным или ушло в фон',
  },
  // Нажали клавишу, объявленную строкой keybind в настройках плагина.
  // Отдельно от ui.addHotkey: там сочетание задаёт плагин, здесь — человек.
  'keybind': {
    permission: null,
    payload: '{ key, combo }',
    when: 'нажато сочетание, выбранное человеком в настройках этого плагина',
  },
  // v1.471.0: что стало с областью плагина. Закрыть её может человек — и плагин
  // обязан об этом узнать, иначе он будет думать, что его окно всё ещё открыто,
  // и рисовать в никуда.
  'app': {
    permission: 'apps',
    payload: '{ id, mode, open, width, height }',
    when: 'твоё окно открылось, сменило вид или его закрыл человек',
  },
  // v1.474.0: клавиши в СВОЁМ окне.
  //
  // Зачем отдельно от ui.addHotkey. Там сочетание обязано быть с Ctrl или Alt —
  // иначе плагин отобрал бы у человека обычные буквы по всему приложению. Игре
  // же нужны стрелки и пробел, и никакой Ctrl тут не поможет.
  //
  // ПОЧЕМУ ЭТО НЕ КЛАВИАТУРНЫЙ ШПИОН. Событие приходит, только пока фокус
  // ВНУТРИ окна этого плагина: набор в чате, поиск, любое поле ввода — не его
  // окно, и туда оно не заглядывает. Плагин не может ни забрать фокус себе, ни
  // подписаться на клавиши без открытого окна.
  'key': {
    permission: 'apps',
    payload: '{ id, key, code, down, repeat, shift, ctrl, alt }',
    when: 'нажата клавиша, пока фокус внутри твоего окна (Esc не приходит — он закрывает окно)',
  },
  // v1.473.0: геймпад. Приходят только ИЗМЕНЕНИЯ: нажали, отпустили, сдвинули
  // ручку. Полное состояние шестьдесят раз в секунду будило бы воркер впустую.
  // Пока на это никто не подписан, приложение геймпады вообще не опрашивает.
  'gamepad': {
    permission: 'input',
    payload: "{ kind: 'connect'|'disconnect'|'button'|'axis', index, id, which, pressed, value }",
    when: 'на геймпаде нажали кнопку, двинули ручку, подключили или отключили его',
  },
}

/** Имена событий — для проверок и подсказок. */
export const PLUGIN_EVENT_NAMES = Object.keys(PLUGIN_EVENTS)

export interface PluginManifest {
  id: string            // латиница/цифры/дефис, уникален — по нему хранится и обновляется
  name: string
  version: string       // semver-подобная строка, для сравнения при обновлении
  author: string
  description: string
  permissions: Permission[]
  hosts: string[]       // домены для 'net'; без 'net' игнорируется
  /** Своя картинка плагина, https (v1.349.0). Пусто — рисуется значок по умолчанию. */
  icon: string | null
  /** Шапка карточки плагина, https. Пусто — мягкая заливка от акцента. */
  banner: string | null
}

export interface InstalledPlugin {
  manifest: PluginManifest
  code: string
  enabled: boolean
  installedAt: string   // ISO
  /** Откуда приехал — для строки «от @ник» в списке. null = поставлен из файла. */
  sourceUserId: string | null
  /**
   * Сделан здесь, в конструкторе (v1.363.0).
   *
   * Раньше «свой» вычислялось по совпадению @author с твоим ником — но автора
   * пишет тот, кто собрал плагин, и чужой плагин с твоим именем в шапке попадал
   * в «Свои». Пометку ставит только конструктор, подделать её из файла нельзя.
   * Необязательное: у плагинов, поставленных до этой версии, его нет, и для них
   * работает прежнее правило.
   */
  authoredHere?: boolean
  /** Настройки самого плагина (то, что он пишет через ponoi.storage). */
  storage: Record<string, unknown>
}

/** Ошибка разбора .ponoi-файла — текст показывается человеку как есть. */
export class PluginParseError extends Error {}
