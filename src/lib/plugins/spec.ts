import { PLUGIN_EVENTS } from './types'
// v1.360.0: полное описание формата плагина — одним текстом.
//
// Зачем отдельным файлом. Человек, который не пишет код, всё равно может сделать
// плагин: скопировать этот текст, отдать любому ИИ и попросить «сделай мне
// плагин, который…». Но для этого описание должно быть полным — если в нём нет
// половины методов или не сказано, что разрешения обязательны, ИИ напишет
// правдоподобный код, который не запустится, и человек решит, что сломано
// приложение.
//
// Поэтому текст лежит здесь, а не в разметке справки, и проверяется тестом:
// в нём обязаны быть упомянуты ВСЕ разрешения и ВСЕ методы, какие понимает
// диспетчер (src/lib/plugins/api.ts). Забыли задокументировать новый метод —
// падает npm run test:plugins, а не выясняется через полгода.


// Таблица событий для документации — из PLUGIN_EVENTS, чтобы описанное и
// разрешённое не разошлись (v1.397.0).
const EVENT_DOC_TABLE = [
  '| событие | когда | что приходит | нужно |',
  '|---|---|---|---|',
  ...Object.entries(PLUGIN_EVENTS).map(([name, e]) =>
    `| ${name} | ${e.when} | ${e.payload} | ${e.permission ?? '—'} |`),
].join('\n')

export const PLUGIN_SPEC = `# Формат плагина Ponoi

Ponoi — мессенджер. Плагин к нему — ОДИН текстовый файл с расширением .ponoi,
внутри обычный JavaScript. Ниже — всё, что нужно знать, чтобы написать рабочий
плагин. Отвечай готовым файлом целиком, без пояснений вокруг.

## Устройство файла

    /**
     * @name Название для списка
     * @id my-plugin
     * @version 1.0.0
     * @author имя автора
     * @description Одна строка о том, что делает
     * @permissions commands, messages.write, notify
     */
    function onLoad(ponoi) {
      // здесь всё, что делает плагин
    }

Шапка — комментарий /** ... */ в самом начале файла, до кода.
Дальше — функция onLoad, ей приложение передаёт объект ponoi.

## Поля шапки

| поле | обязательно | что это |
|---|---|---|
| @id | да | латиница, цифры, дефис. Опознаёт плагин при обновлении, у разных плагинов должен различаться |
| @name | да | как называется в списке |
| @version | да | вида 1.0.0, по нему видно, что обновление новее |
| @author | нет | имя автора |
| @description | нет | одна-две строки |
| @permissions | нет | через запятую, см. ниже |
| @hosts | нет | домены через запятую, нужны только для сети |
| @icon | нет | https-ссылка на картинку |

ВАЖНО: всё, чего нет в @permissions, плагину запрещено. Вызов без разрешения
не молчит — он бросает ошибку. Перечисляй ровно то, что используешь: лишние
разрешения человек видит при установке, и лишнее его отпугнёт.

## Разрешения

| разрешение | что открывает |
|---|---|
| ui | свои кнопки, пункты меню, страница настроек, вопросы человеку |
| css | свои стили оформления |
| commands | свои команды в чате (/имя) |
| messages.read | получать события о новых сообщениях в открытом канале |
| messages.write | отправлять сообщения от имени человека |
| storage | своё хранилище на этом устройстве |
| net | запросы в интернет, только на домены из @hosts |
| settings | своя страница настроек |
| notify | всплывающие уведомления |
| voice | менять эффект своего голоса в звонке |
| context | знать имя человека и какой канал открыт |
| panel | своя панель в плеере, Трекотеке или колонке слева |
| music | видеть, что играет, и управлять плеером |

## Что умеет объект ponoi

Все методы возвращают промис — пиши await.

### Команды (нужно commands)

    ponoi.commands.register('привет', 'Поздороваться', async (arg) => {
      // arg — всё, что человек написал после команды, строкой
    })

### Сообщения

    await ponoi.messages.send('текст')      // нужно messages.write

Отправка ограничена: не чаще 5 сообщений за 10 секунд.

### События

    ponoi.on('message', (msg) => { ... })

Подписка возвращает промис: если разрешения нет, он отклонится — это видно сразу,
а не «обработчик молчит и непонятно почему». Имя события не из списка ниже тоже
отвергается, а не подписывается в никуда.

${EVENT_DOC_TABLE}

### Своя панель в приложении (нужно panel)

Панель — это твой уголок в готовом экране. Ты описываешь строки, а рисует их
само приложение: своей разметки в окно плагин не добавляет никогда.

    ponoi.ui.addPanel({
      slot: 'player',            // 'player' | 'library' | 'sidebar'
      title: 'Мой уголок',
      rows: [                    // те же строки, что и в настройках, до 20
        { type: 'toggle', key: 'on', label: 'Включить', value: true },
        { type: 'button', key: 'go', label: 'Сделать', onClick: async () => {} },
      ],
    })

Места (slot):

| slot | где это |
|---|---|
| player | в плеере, под обложкой |
| library | в Трекотеке, над списком треков |
| sidebar | в колонке слева, под списком каналов |

Одна панель на место от одного плагина; в одном месте — не больше трёх панелей
от всех плагинов сразу.

### Музыка (нужно music)

    const now = await ponoi.music.now()
    // now: { id, title, author, playing, at, duration } либо null, если плеер закрыт

    const все = await ponoi.music.library()
    // [{ id, title, author, plays }] — то же, что видно в Трекотеке

    await ponoi.music.play()
    await ponoi.music.pause()
    await ponoi.music.next()
    await ponoi.music.prev()
    await ponoi.music.queue(trackId)   // поставить следующим; false — нет такого трека
    await ponoi.music.add(url)         // добавить ссылку в Трекотеку

Управлять можно не чаще 20 раз за 10 секунд, добавлять треки — не чаще 5 раз в
минуту: Трекотека общая, и плагин с ошибкой в цикле завалил бы её у всех.
Самого звука плагину не достаётся ни в каком виде.

### Кнопки и меню (нужно ui)

    ponoi.ui.addComposerButton({ label: 'Текст', icon: 'star', onClick: async () => {} })
    ponoi.ui.addMessageAction({ label: 'Что-то', icon: 'star', onClick: async (msg) => {} })
    ponoi.ui.addSettingsPage({ title: 'Мой плагин', rows: [
      { type: 'toggle', key: 'on', label: 'Включить', value: true },
      { type: 'text', key: 'name', label: 'Имя', value: '' },
      { type: 'select', key: 'mode', label: 'Режим', value: 'a',
        options: [{ value: 'a', label: 'Первый' }, { value: 'b', label: 'Второй' }] },
      { type: 'button', key: 'go', label: 'Сделать', onClick: async () => {} },
    ] })

Кнопок и пунктов меню — не больше 5 каждого вида, команд — не больше 15.

### Спросить человека (нужно ui)

    const да = await ponoi.ui.confirm({ title: 'Точно?', text: 'Пояснение', ok: 'Да' })
    const имя = await ponoi.ui.prompt({ title: 'Как тебя звать?', placeholder: 'имя' })
    // prompt вернёт null, если человек отказался

Своё окно плагин нарисовать не может и не должен: он подделал бы им любое окно
приложения. Окно рисует Ponoi, плагин получает только ответ.

### Хранилище (нужно storage)

    await ponoi.storage.set('ключ', любое_значение)
    const v = await ponoi.storage.get('ключ')
    await ponoi.storage.remove('ключ')
    const keys = await ponoi.storage.keys()

Одно значение — не больше 64 КБ. Хранится на этом устройстве, между устройствами
не синхронизируется.

### Обстановка (нужно context)

    const me = await ponoi.me()        // { id, name } или null
    const ch = await ponoi.channel()   // { id, name, serverId, serverName } или null

### Уведомления (нужно notify)

    await ponoi.notify('текст')

Не чаще 10 раз за 10 секунд.

### Буфер обмена (нужно ui)

    await ponoi.clipboard.write('текст')

### Оформление (нужно css)

    ponoi.css('.msg-txt { font-style: italic }')

Стили применяются ко всему приложению. Селекторы приложения могут меняться между
версиями — не рассчитывай на них слишком сильно.

### Сеть (нужно net и @hosts)

    const r = await ponoi.net.fetch('https://api.example.com/x')

Только https и только домены, перечисленные в @hosts. Не чаще 20 запросов за
10 секунд, ответ — не больше 1 МБ, ожидание — 10 секунд.

### Голос в звонке (нужно voice)

    const list = await ponoi.voice.list()      // доступные эффекты
    const now = await ponoi.voice.current()
    await ponoi.voice.setEffect('robot')       // false, если звонка сейчас нет

Плагин только выбирает эффект: сам звук обрабатывает приложение, ни одного
сэмпла разговора плагину не достаётся.

### Отладка

    ponoi.log('что угодно')     // видно в журнале плагина (настройки → Плагины → значок кода)
    ponoi.warn('осторожно')     // то же, но жёлтым
    ponoi.error('всё плохо')    // то же, но красным

Журнал держит последние 200 строк, туда же попадают ошибки самого плагина.
Разрешения на это не нужно: наружу вывод не уходит.

## Чего в плагине нет

Плагин работает в изолированном потоке. Ему НЕДОСТУПНЫ: window, document, DOM,
localStorage, cookie, обычный fetch и XMLHttpRequest, eval, доступ к другим
плагинам, к базе данных Ponoi и к токену сессии человека. Всё общение с
приложением — только через объект ponoi. setTimeout и setInterval работают.

Не пытайся обойти это — не получится, и плагин просто не запустится.

## Правила, которые проверяет приложение

1. Файл должен начинаться с шапки-комментария.
2. onLoad — обычная функция (не стрелочная переменная, не export).
3. @id — только латиница, цифры и дефис.
4. Вызов без разрешения бросает ошибку, а не возвращает пустоту — оборачивай в
   try/catch то, в чём не уверен.
5. Ошибка внутри onLoad не даёт плагину загрузиться; ошибка позже — только
   записывается, плагин продолжает работать.

## Пример целиком

    /**
     * @name Приветствие
     * @id hello-plugin
     * @version 1.0.0
     * @author Ponoi
     * @description Здоровается по команде /привет и помнит, сколько раз
     * @permissions commands, messages.write, storage, notify, context
     */
    function onLoad(ponoi) {
      ponoi.commands.register('привет', 'Поздороваться', async (arg) => {
        const n = (await ponoi.storage.get('count')) || 0
        await ponoi.storage.set('count', n + 1)
        const me = await ponoi.me()
        const кому = arg || (me && me.name) || 'мир'
        await ponoi.messages.send('Привет, ' + кому + '! Раз номер ' + (n + 1))
      })
      ponoi.notify('Плагин приветствия загрузился')
    }

## Как отдать результат

Верни ОДИН файл целиком: шапка, потом onLoad. Без markdown-обёртки, без
пояснений до и после. Человек скопирует его целиком в конструктор плагина Ponoi.
`

/**
 * Просьба, с которой человек начинает разговор с ИИ (v1.417.0).
 *
 * Зачем она такая длинная. Раньше здесь было три строки «прочитай описание и
 * напиши плагин», и ИИ раз за разом выдавал правдоподобный код, который не
 * запускался: он дописывал import'ы, лез в document и fetch, забывал
 * @permissions, возвращал ответ в markdown-обёртке. Всё это ловится только
 * тем, что сказано ЗАРАНЕЕ, — а человек, который просит плагин, обычно и не
 * знает, о чём предупреждать.
 *
 * Здесь только про устройство плагинов. Ни про Ponoi вообще, ни про боты, ни
 * про то, «какой хороший мессенджер»: лишнее в просьбе — это лишнее в ответе.
 */
export const AI_PROMPT_PREFIX =
  'Ты пишешь плагин для мессенджера Ponoi. Ниже — полное описание формата: прочитай его целиком, ' +
  'прежде чем писать код.\n\n' +
  'Главное про устройство, чтобы не написать неработающее:\n' +
  '1. Плагин — это ОДИН файл: блок /** ... */ с полями @name, @id, @version, @author, @description, ' +
  '@permissions, и дальше обычный JavaScript. Никаких сборщиков, файлов рядом и внешних библиотек.\n' +
  '2. Код выполняется в Web Worker. Там НЕТ document, window, localStorage, fetch, XMLHttpRequest, ' +
  'WebSocket, importScripts и любого доступа к странице. Обращение к ним — не «не сработает», а ' +
  'сразу ошибка. Всё, что можно, доступно через объект ponoi, который приходит аргументом в onLoad.\n' +
  '3. Точка входа — function onLoad(ponoi). Никаких import и export: файл выполняется как обычный ' +
  'код, а не как модуль.\n' +
  '4. Каждое действие требует разрешения, и разрешения перечисляются в @permissions. Не выданное ' +
  'разрешение — отказ во время работы, а не молчание. Проси РОВНО то, что используешь: лишнее ' +
  'человек видит при установке и справедливо пугается.\n' +
  '5. Почти всё в ponoi возвращает промис — пиши await. Подписка на события тоже: если разрешения ' +
  'нет, промис отклонится, и это надо ловить, а не оставлять висеть.\n' +
  '6. Сеть — только ponoi.net.fetch и только на домены, перечисленные в @hosts. Домен, которого ' +
  'нет в @hosts, будет отвергнут.\n' +
  '7. Своей разметки в окно плагин не добавляет никогда. Кнопки, панели и страницы настроек ' +
  'ОПИСЫВАЮТСЯ данными (см. ui.addPanel и ui.addSettingsPage), а рисует их само приложение.\n' +
  '8. Имена команд, ключи кнопок и id плагина уникальны: занятое имя команды — ошибка, а не тихая ' +
  'подмена чужой.\n' +
  '9. Есть ограничения по частоте: сообщения, уведомления, управление плеером и добавление треков. ' +
  'Плагин, который шлёт в цикле, получит отказ — не пиши циклы без пауз.\n' +
  '10. Отладка — ponoi.log, ponoi.warn, ponoi.error: их видно в журнале плагина в настройках.\n\n' +
  'Теперь сделай плагин, который: [ОПИШИ СВОИМИ СЛОВАМИ, ЧТО ОН ДОЛЖЕН ДЕЛАТЬ]\n\n' +
  'Ответь ОДНИМ файлом целиком, без markdown-обёртки, без пояснений до и после. ' +
  'Если для задуманного не хватает возможностей из описания — не выдумывай методы, ' +
  'а напиши одной строкой, чего именно не хватает.\n\n'

// ── Боты ────────────────────────────────────────────────────────────────────
//
// То же самое для ботов: человек копирует текст, отдаёт ИИ и получает готовый
// код бота. Бот — не плагин: он живёт снаружи, на своём адресе, и общается с
// Ponoi по вебхуку. Поэтому описание отдельное.

export const BOT_SPEC = `# Как написать бота для мессенджера Ponoi

Бот Ponoi — отдельная программа на ЛЮБОМ языке, поднятая на своём https-адресе.
Ponoi шлёт ей события POST-запросом, она отвечает. Внутри Ponoi бот ничего не
исполняет, доступа к приложению у него нет.

Если готовое приложение поднимать негде — в Ponoi есть «бот с ответами»: команды
и ответы на них задаются прямо в настройках, без кода и без сервера. Этот текст —
про полноценного бота со своим кодом.

## Что нужно один раз сделать в Ponoi

1. Настройки → Другое → Боты → «Мои боты» → создать бота.
2. Скопировать ID приложения, токен и секрет вебхука (секрет показывается один раз).
3. Вписать адрес своего сервера в поле Webhook URL (обязательно https).
4. Завести команды бота — имя и описание. Их увидят в подсказке по «/».

## Как приходят события

POST на твой Webhook URL, тело — JSON, заголовок:

    X-Ponoi-Signature: <hmac-sha256 от СЫРОГО тела, ключ — секрет вебхука>

Подпись проверять ОБЯЗАТЕЛЬНО: без неё кто угодно пришлёт боту что угодно.
Считать её надо от сырого тела запроса, а не от разобранного и снова собранного
JSON — иначе подпись не сойдётся.

### Событие: позвали команду

    { "type": "INTERACTION_CREATE",
      "command": "привет",
      "args": { "text": "всё, что человек написал после команды" },
      "channelId": "...", "userId": "..." }

Отвечать надо СИНХРОННО, в теле ответа:

    { "content": "текст ответа" }

Ponoi ждёт ответа не больше 5 секунд и сам отправит его в канал от имени бота.
Пустой ответ — бот промолчит.

### Событие: в канале появилось сообщение

    { "type": "MESSAGE_CREATE",
      "message": { "id": "...", "channel_id": "...", "author": "...",
                   "author_name": "...", "content": "..." } }

Ответ на это событие Ponoi не ждёт. Хочешь написать в канал — обратись к API
сам (ниже). Свои же сообщения бот не получает — эха не будет.

## API: как боту написать самому

    POST https://<твой-проект>.supabase.co/functions/v1/bot-api/messages
    Authorization: Bot <токен бота>
    Content-Type: application/json

    { "channelId": "...", "content": "текст" }

Проверить токен:

    GET https://<твой-проект>.supabase.co/functions/v1/bot-api/me
    Authorization: Bot <токен бота>

Бот пишет только в каналы серверов, где он состоит, и только туда, куда ему
разрешено правами канала. Это проверяет Ponoi, обойти нельзя.

## Пример целиком (Node.js)

    import crypto from 'node:crypto'
    import express from 'express'

    const SECRET = process.env.PONOI_WEBHOOK_SECRET
    const app = express()

    app.post('/ponoi', express.text({ type: '*/*' }), (req, res) => {
      const mine = crypto.createHmac('sha256', SECRET).update(req.body).digest('hex')
      if (mine !== req.get('X-Ponoi-Signature')) return res.sendStatus(401)

      const event = JSON.parse(req.body)
      if (event.type === 'INTERACTION_CREATE' && event.command === 'привет') {
        return res.json({ content: 'Привет, ' + (event.args.text || 'мир') + '!' })
      }
      res.json({})
    })

    app.listen(3000)

## Правила

1. Адрес обязан быть https. Локальные и внутренние адреса Ponoi не примет.
2. Ответ на команду — не дольше 5 секунд, иначе человек увидит ошибку.
3. Текст ответа обрезается до 4000 символов.
4. Токен и секрет — как пароль: в код их не вписывают, держат в переменных
   окружения.
5. Бота нельзя выгнать или забанить как человека — его убирают в настройках
   сервера.

## Как отдать результат

Верни готовый код целиком, с проверкой подписи. Скажи, какие переменные
окружения нужны. Без пояснений вокруг.
`

/** Просьба к ИИ про бота — с местом для задумки, как и у плагинов. */
export const AI_BOT_PROMPT_PREFIX =
  'Ты пишешь бота для мессенджера Ponoi. Ниже — полное описание того, как это устроено. ' +
  'Прочитай его и напиши бота, который делает вот что: [ОПИШИ СВОИМИ СЛОВАМИ, ЧТО ХОЧЕШЬ]. ' +
  'Ответь готовым кодом, с проверкой подписи.\n\n'
